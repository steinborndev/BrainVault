# web — dashboard frontend (placeholder)

Not implemented yet. The dashboard lands in **M3** (SPEC.md §10): React + Vite +
TypeScript, TanStack Query, SSE, responsive and PWA-ready from the start
(SPEC.md §6, §12.3).

This directory exists so the layout in `CLAUDE.md` is real rather than aspirational.
It is deliberately **not** an npm workspace yet — it gets added to the root
`package.json` `workspaces` array when M3 starts, so `npm install` stays fast until then.
